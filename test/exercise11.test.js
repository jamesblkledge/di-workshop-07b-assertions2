var chai = require('chai');
var assert = chai.assert;

/** Delete this line and the one at the bottom when you're ready for exercise 7
 
    suite('Unit Tests Exercise 11 - Array Identitiy',function(){
      // 11 - #isArray vs #isNotArray 
      test('does split return an array?', function() {
        assert.fail('isThisAnArray?'.split(''), 'String.prototype.split() returns an Array');
      })
      test('does indexOf return an array?', function() {  
        assert.fail([1,2,3].indexOf(2), 'indexOf returns a number.');
      });
    })

// Delete this line when you're ready for exercise 11 **/
